FROM postgres:15-alpine

# Default database credentials (override at runtime or via compose)
ENV POSTGRES_DB=handmadeshop
ENV POSTGRES_USER=postgres
ENV POSTGRES_PASSWORD=qwerty

VOLUME ["/var/lib/postgresql/data"]
EXPOSE 5432

